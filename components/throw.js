AFRAME.registerComponent('throw', {
    init: function () {
        const projectile = document.getElementById('js--box');
        const sphere = document.getElementById('js--sphere');

        this.throw = function(){
            

        }
        
        function pythagoras(x1, z1, x2, z2) {
            return Math.sqrt( Math.pow(x1 - x2, 2) + Math.pow(z1 - z2, 2) )
          }
    },
  
    update: function () {
      
    }
  });